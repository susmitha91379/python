class Solution:
    def findCeil(self,root, x):
        # code here
        ans = -1
        while(root is not None):
            if(root.data >= x):
                ans = root.data
                root = root.left
            else:
                root = root.right
        return ans
